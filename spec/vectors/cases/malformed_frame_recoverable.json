{
  "name": "malformed_frame_recoverable",
  "description": "A frame that fails to decode is answered with a recoverable error and does not end the session: the following user message still yields a proposal.",
  "spec": ["6", "11"],
  "layer": "binding",
  "fixture": "co-buyer",
  "llm": [
    [ { "tool_call": { "name": "transaction.coBuyer.remove", "arguments": {} } } ]
  ],
  "authz": { "default": "allow" },
  "bindings": { "grpc": "skip" },
  "frames": [
    { "json": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions/tx_581",
                             "selected_record_id": "tx_581" } } },
    { "raw": "not json" },
    { "json": { "type": "user_message", "text": "remove the co-buyer", "client_msg_id": "c-1" } }
  ],
  "expect": [
    { "type": "error", "code": "malformed_event", "recoverable": true },
    { "type": "action_proposal", "action_id": "transaction.coBuyer.remove" }
  ],
  "executions": []
}
