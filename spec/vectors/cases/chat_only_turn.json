{
  "name": "chat_only_turn",
  "description": "A text-only model turn arrives as one authoritative done:true chat message carrying the full text; delta count is an implementation detail (normalization drops done:false frames).",
  "spec": ["6.3"],
  "layer": "agent",
  "fixture": "co-buyer",
  "llm": [
    [ { "text": "Hello! Open a transaction and I can help." } ]
  ],
  "authz": { "default": "allow" },
  "steps": [
    { "send": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions" } } },
    { "expect": [] },
    { "send": { "type": "user_message", "text": "hi", "client_msg_id": "c-1" } },
    { "expect": [
      { "type": "chat_message", "done": true,
        "text": "Hello! Open a transaction and I can help." }
    ] }
  ],
  "executions": []
}
