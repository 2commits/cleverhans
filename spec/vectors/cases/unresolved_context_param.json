{
  "name": "unresolved_context_param",
  "description": "With no record selected, a required context-sourced param cannot be resolved: the candidate is invalid, nothing renders, and the agent declines conversationally without spending a retry (the failure reflects this moment's app state, not the tool call).",
  "spec": ["4.1", "7.1"],
  "layer": "agent",
  "fixture": "co-buyer",
  "llm": [
    [ { "tool_call": { "name": "transaction.coBuyer.remove", "arguments": {} } } ]
  ],
  "authz": { "default": "allow" },
  "steps": [
    { "send": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions" } } },
    { "expect": [] },
    { "send": { "type": "user_message", "text": "remove the co-buyer", "client_msg_id": "c-1" } },
    { "expect": [
      { "type": "chat_message", "done": true }
    ] }
  ],
  "executions": []
}
