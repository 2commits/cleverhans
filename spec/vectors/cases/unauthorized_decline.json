{
  "name": "unauthorized_decline",
  "description": "An authz denial declines conversationally at propose time — no proposal renders and no retry is spent, because no rephrasing makes an unauthorized action authorized.",
  "spec": ["7.1", "12.9"],
  "layer": "agent",
  "fixture": "co-buyer",
  "llm": [
    [ { "tool_call": { "name": "transaction.coBuyer.remove", "arguments": {} } } ]
  ],
  "authz": { "default": { "deny": "co-buyer changes are restricted" } },
  "steps": [
    { "send": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions/tx_581",
                             "selected_record_id": "tx_581" } } },
    { "expect": [] },
    { "send": { "type": "user_message", "text": "remove the co-buyer", "client_msg_id": "c-1" } },
    { "expect": [
      { "type": "chat_message", "done": true }
    ] }
  ],
  "executions": []
}
