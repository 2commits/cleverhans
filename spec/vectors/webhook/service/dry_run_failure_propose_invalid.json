{
  "name": "dry_run_failure_propose_invalid",
  "description": "§14.5: a 5xx from dry_run during propose-time validation makes the candidate invalid — nothing renders, the agent answers conversationally, and no execute delivery ever happens.",
  "spec": ["14.5", "7.1", "12.7"],
  "layer": "webhook-service",
  "fixture": "co-buyer",
  "service_config": { "secret": "vector-secret" },
  "llm": [
    [ { "tool_call": { "name": "transaction.coBuyer.remove", "arguments": {} } } ]
  ],
  "host": {
    "dry_run": [ { "respond": { "status": 500, "body": { "error": "db down" } } } ]
  },
  "steps": [
    { "connect": {} },
    { "expect_connect": { "status": 101 } },
    { "send": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions/tx_581",
                             "selected_record_id": "tx_581", "view_type": "detail" } } },
    { "expect": [] },
    { "send": { "type": "user_message", "text": "remove the co-buyer", "client_msg_id": "c-1" } },
    { "expect": [
      { "type": "chat_message", "done": true }
    ] }
  ],
  "expect_deliveries": [
    { "endpoint": "verify_session" },
    { "endpoint": "authorize" },
    { "endpoint": "dry_run" }
  ]
}
