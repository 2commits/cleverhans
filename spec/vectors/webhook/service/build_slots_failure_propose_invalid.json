{
  "name": "build_slots_failure_propose_invalid",
  "description": "§14.9 fail-closed: a 5xx from build_slots during propose-time validation makes the candidate invalid — nothing renders, the agent declines conversationally, and no execute delivery ever happens. A wrong card is a trust bug; silence beats guessing.",
  "spec": ["14.9", "7.1"],
  "layer": "webhook-service",
  "fixture": "co-buyer",
  "service_config": { "secret": "vector-secret", "build_slots": true },
  "llm": [
    [ { "tool_call": { "name": "transaction.coBuyer.remove", "arguments": {} } } ]
  ],
  "host": {
    "build_slots": [ { "respond": { "status": 500, "body": { "error": "template service down" } } } ]
  },
  "steps": [
    { "connect": {} },
    { "expect_connect": { "status": 101 } },
    { "send": { "type": "init", "spec_version": "0.1.0-draft",
                "context": { "route": "/transactions/tx_581",
                             "selected_record_id": "tx_581", "view_type": "detail" } } },
    { "expect": [] },
    { "send": { "type": "user_message", "text": "remove the co-buyer", "client_msg_id": "c-1" } },
    { "expect": [
      { "type": "chat_message", "done": true }
    ] }
  ],
  "expect_deliveries": [
    { "endpoint": "verify_session" },
    { "endpoint": "authorize" },
    { "endpoint": "dry_run" },
    { "endpoint": "build_slots" }
  ]
}
