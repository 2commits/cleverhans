{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cleverhans.dev/spec/webhook/v1/verify_session.request.json",
  "title": "verify_session request (SPEC.md §14.3)",
  "type": "object",
  "required": ["webhook_version", "session_id", "headers"],
  "additionalProperties": false,
  "properties": {
    "webhook_version": { "const": 1 },
    "session_id": {
      "type": "string",
      "description": "Service-generated, opaque, unique per envelope stream."
    },
    "headers": {
      "type": "object",
      "description": "The client's stream-establishment headers, restricted to the configured forward-allowlist (default: authorization, cookie). Keys lowercased.",
      "additionalProperties": { "type": "string" }
    }
  }
}
