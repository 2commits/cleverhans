// AUTO-GENERATED by cleverhans-codegen — do not edit.
// Regenerate from the Rust action registry (spec §9: one source, three consumers).

export type ActionId = "document.archive" | "document.publish" | "document.rename" | "documents.deleteByStatus";

export type BlockType = "bulk_preview" | "confirm";

/** Slots of block type `bulk_preview`. */
export interface BulkPreviewSlots {
  title: string;
}

/** Slots of block type `confirm`. */
export interface ConfirmSlots {
  title: string;
  detail?: string;
}

/** Filled params of `document.archive`. */
export interface DocumentArchiveParams {
  /** source: context */
  documentId: string;
}

/** Filled params of `document.publish`. */
export interface DocumentPublishParams {
  /** source: context */
  documentId: string;
}

/** Filled params of `document.rename`. */
export interface DocumentRenameParams {
  /** source: context */
  documentId: string;
  /** source: utterance */
  title: string;
}

/** Filled params of `documents.deleteByStatus`. */
export interface DocumentsDeleteByStatusParams {
  /** source: utterance */
  status: "draft" | "published" | "archived";
}

export interface BlockSlotsMap {
  "bulk_preview": BulkPreviewSlots;
  "confirm": ConfirmSlots;
}

export interface ActionParamsMap {
  "document.archive": DocumentArchiveParams;
  "document.publish": DocumentPublishParams;
  "document.rename": DocumentRenameParams;
  "documents.deleteByStatus": DocumentsDeleteByStatusParams;
}

export const ACTIONS: {
  [K in ActionId]: { blockType: BlockType; mutates: boolean };
} = {
  "document.archive": { blockType: "confirm", mutates: true },
  "document.publish": { blockType: "confirm", mutates: true },
  "document.rename": { blockType: "confirm", mutates: true },
  "documents.deleteByStatus": { blockType: "bulk_preview", mutates: true },
};
